{# {{ items }} #}

<!-- форма отправки -->
<form action="" method="post">
  {{ items.form_build_id }}
  {{ items.form_id }}
  {{ items.form_token }}

  <div class="form">{{ items.name }}</div>
  <div class="form">{{ items.gender }}</div>
  <div class="form">{{ items.about }}</div>
  <div class="form">{{ items.city }}</div>
  <div class="form">{{ items.save }}</div>
</form>
